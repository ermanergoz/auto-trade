[pytest]
# Marker registration for the LLM-07 gate eval suite (avoids PytestUnknownMarkWarning).
# Keep the existing CI command `pytest tests/ -v` fully compatible — this file only
# registers markers; it does not narrow testpaths or change collection.
markers =
    tier1: pure-code hard gates for core.gate.gate_signal (enum, veto-only, verbatim-citation, injection) — deterministic, no provider key
