"""LLM-07 versioned gate eval suite (tests.eval).

Package marker so `tests.eval.*` is importable — required because `tests` is a
regular package. Enables `from tests.eval.conftest import ...`,
`import tests.eval.rubric`, and `python -m tests.eval.run_gate_eval`.
"""
